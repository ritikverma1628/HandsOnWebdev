# Forms in React

###1. Learning to discard forms from using their internal states and giving them the react states.
###2. The form components given the react states are then called the controlled components
###3. This is done using the state variable inside the value attribute of the input element.